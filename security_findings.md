# Tech 7 Security Findings

## Finding Template
| ID | Severity | Area | Route/File | Evidence | Impact | Recommendation | Status |
|---|---|---|---|---|---|---|---|

## Confirmed Findings
| ID | Severity | Area | Route/File | Evidence | Impact | Recommendation | Status |
|---|---|---|---|---|---|---|---|
| SEC-001 | Critical | Supabase RLS | `public.orders`, `order_items`, `payments`, `provider_oauth_tokens`, `service_orders`, `coupons`, `carts`, `shipping_quotes`, others | Before: Supabase MCP showed RLS disabled and public grants on sensitive tables. After: `_validation/security-remediation/after/supabase-after-summary.md` shows RLS enabled and no direct `anon`/`authenticated` grants on 13 sensitive tables; `SET LOCAL ROLE anon; SELECT count(*) FROM public.orders` returned permission denied. | Direct PostgREST access to orders/PII/payment/shipping/service data is blocked for public roles. | Keep access through server/service-role APIs only; add narrow user-scoped policies later only if direct Supabase client access becomes a product requirement. | fixed in Supabase |
| SEC-002 | Critical | Supabase sensitive data | `public.provider_oauth_tokens` | Before: table exposed via API without RLS and contained token columns. After: table has RLS enabled and no direct `anon`/`authenticated` grants. | Direct public access to provider OAuth token rows is blocked. | Preferred future hardening: move token storage to a private schema and keep service-only access. | fixed in Supabase, private-schema move pending |
| SEC-003 | Medium | Supabase storage | Storage bucket `product-images` | Before: `storage.objects` policy `Public read product images` allowed broad SELECT for bucket. After: validation query returned `product_image_public_read_policies = 0`. | Public listing through `storage.objects` policy is removed. | Verify public object URLs in production after deploy/asset smoke test; recreate rollback policy only if public object serving unexpectedly breaks. | fixed in Supabase |
| SEC-004 | Medium | Supabase functions | `public.olx_private_header_ok()` | Before: anon/authenticated had `EXECUTE` and 32 OLX policies referenced the `SECURITY DEFINER` function. After follow-up migration: execute grants for anon/authenticated are empty, policies referencing the function = 0, OLX private-table direct grants for anon/authenticated = 0. Evidence: `_validation/security-remediation/after/supabase-after-followup-2026-06-22.md`. | Public RPC execution path removed. Direct OLX PostgREST access through previous header-secret pattern is blocked. | Keep OLX integration server-side/service-role only; do not reintroduce public header-secret DB policies. | fixed in Supabase |
| SEC-005 | Low | Supabase schema hardening | Extension `pg_trgm`; function `public.set_updated_at` search_path mutable | After migrations, `public.set_updated_at()` includes `SET search_path TO 'public'`; `pg_trgm` extension schema is `extensions`; anon product read/title search still works. Evidence: `_validation/security-remediation/after/supabase-after-followup-2026-06-22.md`. | Function search-path and public-extension hardening warnings addressed. | Keep extension references schema-safe if future SQL calls `similarity()`/trigram functions directly. | fixed in Supabase |
| SEC-006 | High | Public files/static server | `/.git/config`, `/_validation/validate-api-security.json`, `/backup/`, `/.env`, `/node_modules/...` | Local HTTP test before fix showed `/.git/config` returned 200 with git config content and `_validation` JSON returned 200. After fix, all tested sensitive paths return 404 while `/` and `/admin.html` still return 200. Evidence: `_validation/security-audit/http-defensive-results.json` and `http-defensive-results-after-fix.json`. | Repository metadata, validation artifacts, backups or dependency files could be exposed if deployed/run with repo root static serving. | Added sensitive path middleware in `server/app.js` before static/fallback serving. Keep `_validation`, `.git`, dotfiles, backups and `node_modules` out of deployed artifacts too. | fixed locally |
| SEC-007 | Medium | Headers/CSP | `server/app.js`, `vercel.json` | Before: CSP was report-only and included `'unsafe-eval'`. After: `Content-Security-Policy` is enforced and browser evidence shows no `'unsafe-eval'`. Static scan found 43,109 inline script tags, 13,489 inline style/style attributes, and 7,771 inline event attributes, so removing `'unsafe-inline'` in this pass would break the mirrored static storefront. | Browser now enforces CSP and blocks eval, but inline allowance remains a structural limitation. | Treat full no-inline CSP as a separate static-site refactor: extract inline scripts/styles or generate per-page hashes/nonces before removing `'unsafe-inline'`. | fixed to safe stage; no-inline refactor pending |
| SEC-008 | Low | Admin login | `/api/admin/login` | `npm run validate:api-security` now includes `admin login failures do not enumerate username or password`; wrong username and wrong password both return `401 invalid_credentials`. | Username enumeration via login response removed. | Keep generic public failures and rate limiting; log details server-side only if needed. | fixed locally |
| SEC-009 | High | Backend legacy API auth | `backend/src/routes/products.js`, `backend/src/routes/orders.js` | Static review found `POST/PUT/DELETE /api/products` and `GET /api/orders` without auth in the separate `backend/` app. After fix, local backend retest on port 3108 returns `401 missing_token`. Evidence: `_validation/security-audit/backend-defensive-results-after-fix.json`. | If the separate backend is deployed, unauthenticated users could mutate catalog or list customer/order PII. | Added shared `adminAuth` middleware and applied it to product mutations and order listing. | fixed locally |
| SEC-010 | High | Backend legacy static server | `backend/server.js` | Static review found `backend/server.js` serving repo root with `express.static`. After fix, local backend retest returns `404` for `/.git/config` and `/_validation/...` while `/` still returns 200. | Same repository/artifact exposure risk as SEC-006 if backend app is run/deployed. | Added sensitive path middleware before backend static serving. | fixed locally |

## Observations
- `@chrome` direct tool is not available in this Codex session; local Google Chrome was used through Playwright fallback for browser evidence.
- ONE plugin is not available in this Codex session; Supabase MCP is available for project/schema/RLS/advisor checks.
- Ruflo is available: `ruflo v3.12.4`.
- `.env` and `.env.local` exist locally but are not tracked by Git; `.gitignore` covers `.env*`. Values were not printed.
- Root and backend dependency audits reported 0 production vulnerabilities.
- Supabase ONE/`one` was not exposed in this session; Supabase MCP fallback was used and project `lzsaaufsdcmqlasjrqck` was verified before changes.

## Evidence Artifacts
- Planned evidence folder: `_validation/security-audit/`
- Remediation evidence folder: `_validation/security-remediation/`
